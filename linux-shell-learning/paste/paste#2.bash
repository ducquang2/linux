paste -sd ';;\n'

# paste -d';' - - -

# - - - is for merging a file by pasting the data into 3 columns. In this case it is seperated by semicolon seperator.

# As in our problem statement says to make three consecutive rows folded into one line. So - - - does this task for us.